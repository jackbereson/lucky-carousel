services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=mongodb://host.docker.internal:27017/houze-lucky-wheel
      - PORT=3000
      - JWT_SECRET=houze-lucky-wheel-jwt-secret-2024
    restart: unless-stopped
